/**
 * Game feature toggles — flip flags here to enable/disable flows without deleting code.
 */

/** Show only the case dossier; skip teaching questions and final engagement. */
export const SKIP_BRIEFING_CALIBRATION_AND_ENGAGEMENT = true;